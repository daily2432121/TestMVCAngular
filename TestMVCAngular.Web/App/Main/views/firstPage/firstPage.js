(function () {
    var app = angular.module('app');

    var controllerId = 'app.controllers.views.firstPage';
    app.controller(controllerId, [
        '$scope', function ($scope) {
            //...
        }
    ]);
})();